/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: [
            "picsum.photos",
            "encrypted-tbn0.gstatic.com"
        ]
    }
};

export default nextConfig;
